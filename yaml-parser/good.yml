name: build-and-test

stages:
  - build
  - test

jobs:
  compile:
    stage: build
    image: python:3.11
    script: pip install -e .

  unit-tests:
    stage: test
    image: python:3.11
    script: pytest
    needs:
      - compile